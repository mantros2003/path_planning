local_costmap:
    global_frame: odom
    robot_base_frame: base_link
    update_frequency: 10.0
    publish_frequency: 10.0
    static_map: false # Does not use the static map
    rolling_window: true # Moves with the robot
    width: 3.0
    height: 3.0
    resolution: 0.05

    transform_tolerance: 0.5
