# Disable standard recovery behaviors to keep it simple while testing
recovery_behavior_enabled: true
clearing_rotation_allowed: true

# The distance the robot must move before resetting oscillation flags
oscillation_timeout: 10.0
oscillation_distance: 0.2

# Which planner to use as a fallback if needed
controller_patience: 15.0
planner_patience: 5.0
